extends Node2D

	
#func _draw():
#	var radius = 48
#	draw_circle(Vector2.ZERO, radius, Color(1, 0, 0, 0.3))   # wypełnienie
#	draw_arc(Vector2.ZERO, radius, 0, TAU, 64, Color.RED, 2) # obrys

#unc _process(delta):
#	queue_redraw()  # odświeżanie rysunku (np. gdy w przyszłości zmieni się pozycja/promień)
 
